import Testing
import Foundation
@testable import XCAFillLibrary

@Suite("XCAFill Tests")
struct XCAFillTests {
    @Test func example() {
        // Basic test to verify it builds and runs
        #expect(true)
    }
}
